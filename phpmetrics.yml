metrics:
    # paths to explore
    path:
        directory: .
        extensions: php
        exclude: vendor|sandbox|env

    # report and violations files
    logging:
        report:
            html:   ./build/logs/phpmetrics.html
            xml:   ./build/logs/phpmetrics.xml
            csv:   ./build/logs/phpmetrics.csv
        violations:
            xml:    ./build/logs/violations.xml
        chart:
            bubbles: ./build/logs/bubbles.svg
